// footer.js

document.addEventListener('DOMContentLoaded', () => {
    const footer = `
        <footer>
            <lol>
        </footer>
    `;
    document.body.insertAdjacentHTML('beforeend', footer);
});
